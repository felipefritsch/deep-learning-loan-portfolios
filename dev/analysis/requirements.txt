# Analysis/modelling phase deps (Phases 1–3), on top of dev/pipeline/requirements.txt.
# Pinned: the venv runs Python 3.9, whose support ends at these lines (pandas 3.0
# and matplotlib 3.10 both require >=3.10), so floating would break on a 3.9 env.
# pandas: RAM-sized EDA slices, the M3 memo, and the optional df.to_latex() path
#         (the table writer in eda_common.py stays dependency-free regardless).
# matplotlib: M2+ figures via eda_common.save_figure().
pandas==2.3.3
matplotlib==3.9.4
# torch: the Phase-2 models (M7 logit onward) — the shared loss/eval path and every net
#        run through torch (torch_common.py). CPU/MPS on the mac dev box; the cloud-GPU
#        loop (M8+) installs the CUDA build separately.
# scikit-learn: M7 logit numerical cross-check (LogisticRegression vs the torch logit);
#        pulls scipy as a transitive dep.
# lightgbm: M16+ GBT baseline (gbt.py) — the non-neural flexible learner (06_GBT_BASELINE);
#        CPU-only multiclass softmax, reuses scipy from scikit-learn.
torch==2.8.0
scikit-learn==1.6.1
lightgbm==4.6.0
